import React from "react";

const SplashScreen: React.FC = () => (
  <div className="tea-splash-screen" role="status" aria-label="Loading Tea Atelier">
    <div className="tea-splash-mark">TA</div>
    <p className="tea-splash-kicker">Tea Atelier</p>
    <p className="tea-splash-caption">Where every leaf tells a story</p>
  </div>
);

export default SplashScreen;
